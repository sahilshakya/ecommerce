import NavBar from "@/components/NavBar";

import React, { useState } from "react";

const Product = ({}) => {
  return (
    <div>
      <NavBar />
      product
    </div>
  );
};

export default Product;
