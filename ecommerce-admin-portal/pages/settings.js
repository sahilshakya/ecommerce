import Layout from "@/components/Layout";
import React from "react";

const settings = () => {
  return (
    <div className="flex">
      <Layout></Layout>
      settings
    </div>
  );
};

export default settings;
